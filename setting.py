# settings.py

LLM_CONFIG = {
    "model": "bedrock.claude-v2",  # AWS Bedrock Claude model
    # Add other model configs like temperature, max tokens if needed
    # "temperature": 0.7,
    # "max_tokens": 1024
}
